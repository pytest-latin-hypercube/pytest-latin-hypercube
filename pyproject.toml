[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "m4r"
version = "0.1"
dependencies = ["numpy", "pytest"]

[tool.hatch.build.targets.wheel]
packages = ["m4r"]
